# Flask 核心库
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-CORS==4.0.0
Flask-JWT-Extended==4.6.0

# 数据库驱动
PyMySQL==1.1.0
psycopg2-binary==2.9.9        # PostgreSQL 驱动（Vercel + Supabase 使用）
cryptography==41.0.7

# 生产服务器（本地开发用不到，但 Vercel 不需要 gunicorn）
gunicorn==21.2.0

# 工具库
Werkzeug==3.0.1
Pillow==10.1.0
